_TODO: コインの取得
_FIXME: 隠しブロックのみプログラムで当たり判定を調整
_FIXME: 右端に消えていく敵の当たり判定調整
_TODO: ブロックから出るコイン，パワーアップアイテムの実装
_FIXME: ゴールポールの当たり判定を調整
_FIXME: マリオが上端で消えるバグの修正（半分ずつではなくマリオの体全部が一気に消える）
_FIXME: ppuのコードを改善，処理を軽くする（PRINT機能を使わないように変更した．もしまだ重いようならば，処理を分散させる方式も考える）
_FIXME: コインを取得できないバグを修正する
_FIXME: 透明ブロックで，叩いたときのカスタム処理だけ実行されてしまうことがあるので修正する）
_FIXME: 処理が間に合わなかったとき（ラグ発生時）にも_waitDispStatusを実行，ポーズ時の動作も修正
_FIXME: 敵がある程度画面外に行っても処理をする機構（完全なバグ取りができていない可能性あり）
_FIXME: すべての地形の当たり判定実装
_FIXME: 敵の当たり判定フラグを二つに分ける（今は1bitでオブジェクトとスプライトの判定両方をON／OFF）
_TODO: 敵のサイズを可変にする
_FIXME: 倒した敵や画面外の敵が非表示にならないバグを修正
_FIXME: 画面外に出た敵に当たって死なないようにする
_TODO: ヒットボックスの大きさや位置を指定できるようにする
_TODO: 敵の重力や加速度の実装
_TODO: ステージの切り替えシーンの実装
_FIXME: ステージ切り替え時の画面の乱れを修正（多分修正できている）
_TODO: ムービーシーンの実装

TODO: ワールドデータ構造の変更（土管を1byteで配置する，など）
	【現在の動作】
	・x座標とy座標を設定し1ブロックずつ保存してある
	・そこから一列分のブロックデータを生成してバッファに保存
	・フラグが立っていればPPU処理でブロックデータを保存
	【方針】
	・オブジェクトは16*16単位のブロックを集めて使う
	・スタートのx,y座標が必要
	・オブジェクト内のブロックデータ形式は，従来のマップ保存と同じ形式にする
	  → .byte (GET_OBJ_POS x, y), 'B'
	・一列に配置できるオブジェクトは10個にする
		まだ配置が終わっていない（幅が2以上）のオブジェクトを10個まで保存可能にする？
		・オブジェクトのアドレスとx座標，インデックス？
		・もしくは，改行（列）コードを作る
		→オブジェクトのアドレス，インデックス
		→x座標の計算・比較不要
		parts_buff = [
			0: [
				addr: PARTS_COINS_4COL,
				index: 3
			]
		];
		PARTS_COINS_4COL:
			0: $0, '^', NEXT_LINE
			3: $0, '^', NEXT_LINE
			6: $0, '^', NEXT_LINE
			9: $0, '^'
			b: endcode(ff)

		このとき，3:のデータを実行，NEXT_LINEを読み取ったのでindexを6に変更して終了
		また，y座標を下位4bit，上位1bitをNEXT_LINEにしてもいい（$80, '^'）
	・一列分のブロックデータはそのまま使う
TODO: 背景データの作成，表示
TODO: 城壁，ゴール砦など，当たり判定のないオブジェクト作成
TODO: 敵を下から突き上げたときの処理
FIXME: 1マスの穴の処理（クリボーはうまく落ちない，マリオは落ちる）
FIXME: ldarrの使用回数が多い箇所はaddr_tmp1を使って書き直す（何度も呼ばない）
FIXME: 変数の再配置（なるべくグローバルで配置したい）
FIXME: fastcallの仕様，インポートの順番を気にしなくていいようにする→ファイル分割
FIXME: 画面を消す処理を関数化→毎回画面の乱れをなくす処理を書かなくていいようにする
FIXME: マジックコードの消去（エンジンIDなど）

・_init, _everyFrameCalled, _endのような構成になっている関数の構成は変えた方がいいのか，検討する
　-> 現在利用中かのフラグを削ったり，プログラムを小さくできるメリットがあるなら一つにまとめてもよい

現在敵同士の当たり判定にはヒットボックスを利用していない
また，パックンフラワーなどほかの敵とは当たり判定がない（跳ね返らない）敵もあり，その実装をどうするのか


; header
timer*2
height & autowalk 3
level backdrop 3 (water, night, ...)

tile type 2 (mash, color, cloud)
background 2
floor 4

/*
line Y  objy
1    e
2    d
3    c   b
4    b   a
5    a   9
6    9   8
7    8   7
8    7   6
9    6   5
10   5   4
11   4   3
12   3   2
13   2   1
14   1   0
15   0
*/

; tile
1:	xxxxyyyy
	x: Xpos, y: Ypos(4)
2:	pabcdefg
	p: next,
	0 <= y <=11:
		abc != 0:
			abc: type
				1: mash/killer
				2: block_col
				3: hard_col
				4: coin_col
				5: block_row
				6: hard_row
				(7: pipe)
			defg: size
		abc = 7:
			type: pipe
			d: exit enable
			efg: size
		abc == 0: SUBTYPE1
			defg: sub type
				0: Q&powerup
				1: Q&coin
				2: hide&coin
				3: hide&1up
				4: block&powerup
				5: block&ツタ
				6: block&star
				7: block&10coin
				8: block&1up
				9: hard2(used block)
				10: water exit
				11: spring
				12: 複合土管（1-2スタート）
				13: flag pall
	y == 12: SUBTYPE2
		; y座標固定，サイズ可変
		abc: subtype
			0: 落とし穴
			1: ロープでつながったプラットフォーム
			2: 橋(y=7)
			3: 橋(y=8)
			4: 橋(y=10)
			5: 水あり落とし穴
			6: Q(y=3)
			7: Q(y=7)
		defg: size
	y == 15: SUBTYPE3
		; y座標固定
		abc: subtype
			0, 1: vertical rope
			2: castle(level start, end)
			3: stair(size==9でもheightは8に)
			4: 大きい複合土管（1-2終了）
		defg: ??
	y == 13: SP_TYPE1
		a == 1: （まだ実装いらないかな）
			defg: subtype
			0: 複合土管
			1: flag pall
			2: 斧
			3: チェーン
			4: クッパの足場
			; ------------
			5: warp zone
			6, 7: scroll stop
			8: パタパタ（赤）生成
			9: パタパタ（灰色），キラー生成
			10: 生成終了
			11: ループコマンド（迷路用？）
		a == 0:
			cdefg: screen number（1-1で土管出たあとなど）
	y == 14: SP_TYPE2（y==13にまぜてもいいかも）
		a == 1:
			efg: level backdrop
		a == 0:
			bc: background
			defg: floor pattern


; data
PARTS_DEFINE:
	; ATTR:
	;	Ypos: 4bit
	;		Ypos == f: end
	;	repeatX: 1bit
	;	repeatY: 1bit
	;	1block: 1bit
	;	nextLine: 1bit
	; repeatフラグは二つセット可能，その間をリピートする
	.byte 'b', (ATTR $4, 0, 0, 0, 1)
	.byte 'b', (ATTR $f, 0, 0, 0, 0)

; addr-table (split)
PARTS_LO:
	.byte <PARTS_0
		...
	.byte <PARTS_21
PARTS_SP12:
	.byte <PARTS_22
		...

PARTS_HI:
	.byte >PARTS_0
		...
	.byte >PARTS_21
	.byte >PARTS_22

; table for sp obj
PARTS_SP_INDEX_CALC_TABLE:
	.byte PARTS_SP12 - PARTS_LO


; prg
func setAddr(index) {
	addr_hi = PARTS_HI[index]
	addr_lo = PARTS_LO[index]
	; (addr_hi), y = *(PARTS_DEFINE+y)
}
func spObjSet(type, y) {
	; PARTS_SP_INDEX_CALC_TABLEのインデックスは最大4種類
	; → 条件分岐で，定数として読み込んでも良い
	index = type + PARTS_SP_INDEX_CALC_TABLE[y-12]
	setAddr(index, y)
	posY = PARTS_YPOS[index]
}

0 <= y <= 12:
	type = 0x00000abc
	subtype = 0x0000defg
	index = type ? type : subtype + 8
	setAddr(index)
y == 12:
	spObjSet(0x00000abc, y)
y == 13:
	val = 0x000cdefg
	if a == 1:
		if val <= 4:
			spObjSet(val, y)
		else:
			ctrlObjSet(val)
	else:
		setScreenNumber(val)
y == 14:
	if a == 1:
		setLevelBackdrop(0x00000efg)
	else:
		setBg(0x000000bc)
		setFloorPattern(0x0000defg)
y == 15:
	spObjSet(0x00000abc, y)